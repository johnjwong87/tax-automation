import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  basePath: '/apps/S216',
  // assetPrefix: '/apps/S216', // Usually basePath handles this, but including for robustness
};

export default nextConfig;
